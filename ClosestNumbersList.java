import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class ClosestNumbersList {
    public static void main(String[] args) {
        List<Integer> solution = new ArrayList<> (Arrays.asList ());
        System.out.println("Enter numbers, with a blank line to end");
        for (String line; !(line = input.nextLine()).trim().isEmpty(); ) {
            solution.add(Integer.parseInt(line));
        }




    }
}
